
<%= form_with(model: service, name: 'form' ) do |form| %>
  <% if service.errors.any? %>
    <div id="er" class="alert alert-danger" role="alert">
      <ul>
        <% service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <table class="tbl">
    <div class="icon_field">
    <tr>
        <td><%= form.label t('activerecord.attributes.service.icon') %></td>
        <td>
          <select id="icons">
            <% target_folder_path = File.join(Rails.root, "/app/assets/images/Icons") %>
            <% Dir.children(target_folder_path).sort.each do |icon| %>
              <option value="Icons/<%= icon %>" data-imagesrc="/assets/Icons/<%= icon %>"><%= icon %></option>
            <% end %>
          </select>
          <input type="hidden" id="service_icon_url" name="service[icon_url]" />
        </td>
      </tr>
    </div>
    <div class="name_field">
<tr>
    <td><%= form.label t('activerecord.attributes.service.service_name') %></td>
  <td><%= form.text_field :name, placeholder: t('activerecord.attributes.service.placeholder_service_name')%></td>
</tr>
    </div>
    <div class="description-field">
    <tr>
    <td><%= form.label t('activerecord.attributes.service.description')%></td>
    <td><%= form.text_area :description, placeholder: t('activerecord.attributes.service.placeholder_service_description')%></td>
    </tr>
    </div>
    <div class="button">
    <tr>
      <td></td>
      <% if @service.new_record? %>
      <td><%= form.submit t('global.forms.create_button'), class: "btn btn-outline-primary", id: 'but', disabled: true%><%= link_to t('global.forms.all_services') , services_path, class: 'btn btn-outline-secondary', role: 'button'%></td>
        <% else %>
        <td><%= form.submit t('global.forms.update_button'), class: "btn btn-outline-primary", id: 'but', disabled: true%><%= link_to t('global.forms.all_services') , services_path, class: 'btn btn-outline-secondary', role: 'button'%></td>
        <% end  %>
    </tr>
    </div>
    <%=  javascript_pack_tag 'dis_button', 'data-turbolinks-track': 'reload' %>
</table>

<% end %>


