<%= form_with(model: room_image) do |form| %>
  <% if room_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room_image.errors.count, "error") %> prohibited this room_image from being saved:</h2>

      <ul>
        <% room_image.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :room_id %>
    <%= form.text_field :room_id %>
  </div>

  <div class="field">
    <%= form.label :image_id %>
    <%= form.number_field :image_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
