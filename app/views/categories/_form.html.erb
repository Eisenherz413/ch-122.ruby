<%= form_with(model: category) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label for="formGroupExampleInput"><%= form.label :name, class: "fw-light" %></label>
    <input type="email" class="form-control" placeholder="<%= form.text_field :name, disabled: true %>">
    <br>
  </div>

  <form>
  <div class="form-group">
    <label for="exampleInputEmail1"><%= form.label :description %></label>
    <input type="email" class="form-control" placeholder="<%= form.text_field :description %>">
  <br>
  </div>

    <div class="form-group">
      <%= form.submit "Update Category Description", class: "btn btn-outline-secondary" %>
    </div>
  </form>

<% end %>
